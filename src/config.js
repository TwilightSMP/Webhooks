export const ORG = 'TwilightSMP';

export const COLORS = {
  push: 0x9b59b6,
  pull_request: 0xe74c3c,
  issues: 0xf1c40f,
  release: 0x1abc9c,
  heartbeat: 0x2c2f33,
};

export const SMP_ROLES = ['Relic', 'Cursed', 'Dead', 'Wanderer', 'Bound', 'Forsaken'];

export const SMP_FLAVOR = [
  'the realm stirs',
  'a seal has broken',
  'the void speaks',
  'twilight descends',
  'an echo remains',
  'the chronicle updates',
  'fate is rewritten',
  'a shadow moves',
];

export const GITHUB_API = 'https://api.github.com';

export const FETCH_LIMITS = {
  commits: 5,
  pull_requests: 5,
  issues: 5,
  releases: 3,
};

export const STATE_DIR = '../state';

export const STATE_FILES = {
  push: 'push.json',
  pull_request: 'pr.json',
  issues: 'issues.json',
  release: 'release.json',
};

export const HEARTBEAT_ENABLED = true;
